apiVersion: v1
kind: ConfigMap
metadata:
  name: sekha-config
  namespace: sekha
data:
  config.toml: |
    [server]
    port = 8080
    host = "0.0.0.0"
    
    [database]
    url = "sqlite:///data/sekha.db"
    
    [chroma]
    url = "http://chroma:8000"
    
    [redis]
    url = "redis://redis:6379"
    
    [ollama]
    url = "http://ollama:11434"
    
    [logging]
    level = "info"
